[
  {
    "version": "1.9.7",
    "date": "2026-07-11",
    "title": "Show real avatars on Dashboard and Metrics pages, not just the Activity page",
    "features": [
      "v1.9.6 added avatarUrl to the Activity page's user data, but three other surfaces build their user objects from separate code paths that were missed: the Metrics page's User Leaderboard and Watch Streaks panels (sourced from watchActivity.byUser), and the Dashboard's Recent Activity and Top Viewers widgets (the latter also sourced from watchActivity.byUser). All four now carry avatarUrl/useGravatar through from the database and pass it to UserAvatar, so a user's real avatar (e.g. a custom uploaded image) shows consistently across the whole app instead of only on the Activity page."
    ],
    "bugFixes": [],
    "miscChores": []
  },
  {
    "version": "1.9.6",
    "date": "2026-07-11",
    "title": "Stop preview/hover autoplay from counting as a real watch; show real avatars on Activity cards",
    "features": [
      "Activity cards (both grid and list view) now show the actual user avatar (avatarUrl, respecting useGravatar) instead of always falling back to a generic letter badge. Previously the Activity feed's user objects never included avatarUrl, so UserAvatar had nothing to render but initials, even for accounts with a real avatar set (as seen on the Users page)."
    ],
    "bugFixes": [
      "isActuallyWatched() still had a gap after v1.9.3: it checked state.overallTimeWatched > 0, but overallTimeWatched was being set to the same raw position value as timeOffset - so any nonzero position (including a few seconds of Nuvio's hover/preview-autoplay while just browsing the library) still counted as a full watch. The item's actual duration was never even captured from the API response to compare against. getLibrary() now records state.duration alongside timeOffset, and isActuallyWatched requires progress to be at least 5% of the runtime (same threshold used by AIOManager) instead of just nonzero - filters out preview noise while still catching real partial watches. Falls back to requiring a video_id when duration isn't available (e.g. live TV/IPTV items)."
    ],
    "miscChores": []
  },
  {
    "version": "1.9.5",
    "date": "2026-07-11",
    "title": "Label Activity feed items with which Nuvio profile they were watched under",
    "features": [
      "Since v1.9.4 now syncs all Nuvio profiles instead of just the primary one, movie/episode watch entries can come from different profiles on the same account (e.g. a household's Main, Secondary, and Test profiles). Each entry is now tagged with the Nuvio profile name it came from and stored in a new profileLabel column on EpisodeWatchHistory and MovieWatchHistory. The Activity page (both grid and list views) shows this as a small badge on each card when known, so activity from different profiles stays visually distinguishable instead of being merged anonymously."
    ],
    "bugFixes": [],
    "miscChores": []
  },
  {
    "version": "1.9.4",
    "date": "2026-07-11",
    "title": "Sync watch activity across all Nuvio profiles, not just the primary one",
    "features": [],
    "bugFixes": [
      "getLibrary() in the Nuvio provider hardcoded p_profile_id: 1 on every RPC call (sync_pull_library, sync_pull_watch_progress, sync_pull_watched_items), so any watch activity happening under a second, third, etc. Nuvio profile was invisible to SlickSync no matter how long you waited - not a polling delay, the wrong profile was being queried, permanently. Confirmed via direct sync_pull_profiles/sync_pull_watch_progress queries: live playback was landing on a non-primary profile that was never being fetched.",
      "getLibrary() now calls sync_pull_profiles first and fetches library/progress/watched-items for every profile the account has, merging the results (most-recent watch wins if the same title shows progress on more than one profile). Falls back to profile 1 alone if the profile list can't be fetched, so single-profile accounts are unaffected. getAddons()/setAddons() are intentionally left on profile 1 only for now - out of scope for this fix."
    ],
    "miscChores": []
  },
  {
    "version": "1.9.3",
    "date": "2026-07-11",
    "title": "Fix false 'watched' activity for library items that were only saved, not played",
    "features": [],
    "bugFixes": [
      "isActuallyWatched() in both activityMonitor.js and metricsBuilder.js was treating any item with a non-empty state.video_id as watched. Nuvio creates a watch_progress row (with video_id populated, but position 0) the instant an item is bookmarked/saved to the library, not just when it's actually played - so movies added to your Saved library but never opened were being logged as real watch activity in the Activity feed, timestamped to when they were saved. Now requires state.timeOffset > 0 (genuine playback progress) alongside video_id before counting an item as watched."
    ],
    "miscChores": []
  },
  {
    "version": "1.9.2",
    "date": "2026-07-11",
    "title": "Attempt fix for backend logs never appearing in docker logs",
    "features": [],
    "bugFixes": [
      "Confirmed via testing: the backend process (run via bun, backgrounded in start.sh) was producing zero visible output after its startup lines - not even its own \"server running\" log, despite the process functioning completely normally (serving requests successfully, passing health checks). The frontend, also backgrounded but run via a different process, logged normally the whole time. That strongly points to bun fully-buffering stdout/stderr instead of line-buffering it once backgrounded in a non-TTY context, rather than anything wrong with the application code itself - console output was very likely accumulating in an internal buffer that was rarely or never flushed.",
      "Added stdbuf -oL -eL to force line-buffered output on the backend process, and added coreutils to the production image to provide it. Verified stdbuf correctly execs into the wrapped process rather than staying around as a separate parent, so the existing PID-based shutdown/cleanup logic is unaffected. Could not fully verify this resolves bun's specific buffering behavior without bun available to test directly - this is a well-reasoned, standard fix for this exact class of problem, but needs real confirmation after deploying."
    ],
    "miscChores": []
  },
  {
    "version": "1.9.1",
    "date": "2026-07-11",
    "title": "Fix activity monitor silently swallowing errors",
    "features": [],
    "bugFixes": [
      "The scheduled 5-minute activity/metrics check had two bare catch blocks that logged nothing at all (one explicitly commented \"Silently fail - dont spam logs\"). Any genuine, persistent error in that cycle - a bad database query, a broken model reference, anything - was completely indistinguishable from \"nothing new happened this cycle\", with zero trace in the logs. Both now log a warning (and the per-account failure logs the full stack trace) so a real failure is visible instead of silent."
    ],
    "miscChores": []
  },
  {
    "version": "1.9.0",
    "date": "2026-07-11",
    "title": "Fix Activity page and Watch Time Today showing no data",
    "features": [
      "Added movie watch history tracking (a movie equivalent of the existing episode watch history), populated by the same reliable pipeline that already tracks episodes for both Stremio and Nuvio."
    ],
    "bugFixes": [
      "The Activity page's main feed, and the Dashboard's Recent Activity widget, were built entirely from WatchSession data - a heuristic that requires an item's progress to visibly change between two 5-minute polls to register at all, built specifically around Stremio's data shape and confirmed unreliable for both providers in this setup. Both now merge in a new reliable activity feed (built from watch history that a separate, already-proven pipeline populates correctly) for anything sessions missed, deduplicated against session entries so nothing shows twice.",
      "\"Watch Time Today\" on the Activity page, and the Watch Time Trend graph on the Metrics page, were also reading from the same unreliable session data - already-correct totals computed from watch activity data earlier in the same request were being discarded in favor of the sparse session-based numbers. Both now use the reliable totals directly."
    ],
    "miscChores": []
  },
  {
    "version": "1.8.8",
    "date": "2026-07-11",
    "title": "Fix ghost/deleted user still showing in Dashboard Recent Activity",
    "features": [],
    "bugFixes": [
      "The Dashboard's Recent Activity widget reads from the same WatchSession data as the old leaderboard did, and had the identical ghost-user bug: a deleted/recreated connection's leftover session rows still showed up with their raw database ID as a display name. Sessions belonging to users that no longer exist are now filtered out entirely instead of falling back to showing the raw ID, matching the same guard already used elsewhere in the metrics builder."
    ],
    "miscChores": []
  },
  {
    "version": "1.8.7",
    "date": "2026-07-11",
    "title": "Fix Metrics leaderboard and summary showing wrong/missing data",
    "features": [],
    "bugFixes": [
      "The Metrics leaderboard and totals were built from WatchSession data (a hash-comparison heuristic that detects live watching by checking whether an item's progress changed between 5-minute polls) instead of the already-reliable WatchActivity table. In a confirmed real case, a user with 326 real WatchActivity rows had zero qualifying WatchSession rows and was completely missing from the leaderboard, while a deleted/orphaned user's leftover session rows still showed up with their raw database ID as a display name. The summary totals (movies/shows/watch time) were also being reset to 0 and recomputed from this same unreliable session data, discarding correct numbers that had already been computed from WatchActivity earlier in the same request.",
      "Checked both the original upstream project and another Stremio manager for a working session-tracking reference for Nuvio specifically, and confirmed neither actually tracks it - this looks like a broader gap in how session-detection heuristics translate to Nuvio's data shape, not something specific to this fork. Rather than try to extend that heuristic, the leaderboard and summary now build from the same WatchActivity data that was already working correctly elsewhere on the Metrics and Activity pages."
    ],
    "miscChores": []
  },
  {
    "version": "1.8.6",
    "date": "2026-07-10",
    "title": "Fix constant Nuvio token refresh spam and 429 rate limits",
    "features": [],
    "bugFixes": [
      "createProvider() is called fresh on every single operation (every activity check, every sync, every addon fetch) rather than being reused across a session. Without any caching, this forced a brand new Nuvio token refresh on every single one of those calls, even seconds apart, even though a Supabase access token is normally valid for about an hour. That is what caused the constant \"Nuvio token refresh failed\" log spam and 429 rate limit responses - added a module-level token cache (keyed by Nuvio user ID) so a still-valid token gets reused across separate provider instances instead of being refreshed every time.",
      "Also fixed a related race condition: Supabase refresh tokens are single-use and rotate on every refresh, so two concurrent requests for the same user could have raced to refresh with the same (about to be invalidated) token, causing one of them to fail. Concurrent callers for the same user now share one in-flight refresh instead of firing duplicate requests."
    ],
    "miscChores": []
  },
  {
    "version": "1.8.5",
    "date": "2026-07-06",
    "title": "Fix a server crash (undefined AUTH_ENABLED) and two more same-email-different-provider blocks",
    "features": [],
    "bugFixes": [
      "Fixed a server crash: AUTH_ENABLED was referenced in two places (the Stremio /connect endpoint, and the general user-update endpoint) but never actually defined anywhere in the codebase - a leftover from an old refactor that renamed this to INSTANCE_TYPE everywhere else. Replaced both with the same INSTANCE_TYPE check used consistently elsewhere.",
      "While fixing the crash, found the same email-scoping bug from v1.8.4 in two more places: the Stremio /connect endpoint (logging into an existing Stremio account) and the general user-update endpoint (changing a user's email) both checked for ANY existing user with that email instead of scoping to the same provider type. Both now correctly allow a Stremio account and a Nuvio account to share an email."
    ],
    "miscChores": []
  },
  {
    "version": "1.8.4",
    "date": "2026-07-06",
    "title": "Fix: could not have a Stremio and Nuvio account with the same email",
    "features": [],
    "bugFixes": [
      "Creating a Stremio user checked whether ANY user (regardless of provider) already used that email, instead of scoping the check to Stremio accounts specifically. This meant if a Nuvio account already existed with a given email, creating a Stremio account with that same email was silently blocked - the database itself has always correctly allowed the same email across both providers (a composite email+providerType constraint), this was purely an overly broad check in the Stremio creation endpoint. The equivalent Nuvio endpoint already scoped this correctly; the Stremio one now matches it."
    ],
    "miscChores": []
  },
  {
    "version": "1.8.3",
    "date": "2026-07-06",
    "title": "Fix group avatars not showing on the Groups list page",
    "features": [],
    "bugFixes": [
      "The Groups list page (both table and card views) never passed the avatarUrl prop to the Avatar component at all, so custom group photos only ever showed on the group detail page, never in the list. Backend already returned the data correctly - this was purely a missing prop on the frontend."
    ],
    "miscChores": []
  },
  {
    "version": "1.8.2",
    "date": "2026-07-06",
    "title": "Fix custom avatar images rendering huge, and fix drag-drop tab highlighting for real this time",
    "features": [],
    "bugFixes": [
      "Avatar images with a custom className (e.g. group avatars, which add a shadow/ring) lost their size constraint entirely instead of keeping it alongside the custom styling. With nothing capping the dimensions, a real uploaded image rendered at its full natural size instead of the intended small circle.",
      "The v1.8.1 tab-alignment fix addressed a real bug but was not the whole story: the drag system used closestCenter collision detection everywhere, which compares the dragged card's own center to each target's center rather than the actual cursor position - fine for reordering similarly-sized cards, wrong for a card with a far-off center being dropped onto a small, distant tab or the sidebar link. Switched to a hybrid strategy: precise pointer-position detection first, falling back to closestCenter only for the gap-between-cards reordering case."
    ],
    "miscChores": []
  },
  {
    "version": "1.8.1",
    "date": "2026-07-06",
    "title": "Fix Vault category tab drag-drop highlighting the wrong tab",
    "features": [],
    "bugFixes": [
      "Dragging a Vault entry over a category tab was highlighting a different tab than the one under the cursor, and could leave a tab stuck highlighted after ending the drag. Caused by an unstyled wrapper div added around each droppable tab that didn't match the button's own flex sizing, throwing off drag-and-drop hit detection. Fixed by giving the wrapper the same flex sizing the button used to have directly."
    ],
    "miscChores": []
  },
  {
    "version": "1.8.0",
    "date": "2026-07-06",
    "title": "Vault: right-click menu, drag-to-recategorize, drag-to-Addons; bulk Move to Vault",
    "features": [
      "Vault entries now also support right-click for a context menu (Run Check Now, Edit, Open Dashboard, Move to Addons, Delete) alongside the existing icon row",
      "Drag any Vault entry onto a category tab to recategorize it on the spot",
      "Drag any Vault entry onto \"Addons\" in the sidebar to move it there directly",
      "Addons page: select one or more addons and use the new \"Move to Vault\" bulk action in the floating selection bar"
    ],
    "bugFixes": [],
    "miscChores": [
      "Reworked the Vault page drag system onto a single shared DndContext spanning the whole admin layout (not just the page), which is what makes dropping onto the sidebar possible"
    ]
  },
  {
    "version": "1.7.0",
    "date": "2026-07-06",
    "title": "Move addons to/from Vault, drag-and-drop reordering",
    "features": [
      "Addons page: new \"Move to Vault\" action per addon - creates a tracked Vault entry from the addon's manifest URL (with a live reachability check) and removes the addon from active management. If the addon was assigned to any group, it is removed from those too",
      "Vault: new \"Move to Addons\" action per entry - reveals the stored secret, and if it looks like a manifest URL, recreates it as a real addon and removes the Vault entry",
      "Vault entries can now be reordered by drag-and-drop within a single category (select a specific category tab, not All, to enable dragging)"
    ],
    "bugFixes": [
      "Caught and fixed a route-ordering bug in the same commit that introduced it: the new /vault/reorder endpoint was registered after the existing /vault/:id handler, so Express matched \"reorder\" as an :id parameter and the reorder request never reached its real handler"
    ],
    "miscChores": []
  },
  {
    "version": "1.6.0",
    "date": "2026-07-06",
    "title": "Category-specific Vault forms with real login validation",
    "features": [
      "Vault Add Entry form is now category-aware: Stremio/Nuvio categories ask for email+password and validate by actually logging in; Usenet Indexers ask for a Newznab URL + API key; Usenet Providers ask for host/port/username/password/SSL/data cap",
      "Two new active-checks: stremio_auth and nuvio_auth, which log in for real rather than just pinging a server"
    ],
    "bugFixes": [
      "Fixed a real bug where a single failed avatar image load could permanently hide all future valid images for that component instance (imageError state never reset when the src changed)",
      "Added defensive inline max-width to the Modal component and console diagnostics on image load failure"
    ],
    "miscChores": []
  },
  {
    "version": "1.5.1",
    "date": "2026-07-06",
    "title": "Fix avatar bugs: uploaded images 404ing, list page not showing custom avatars, distorted sizing",
    "features": [],
    "bugFixes": [
      "Uploaded avatar images never actually displayed: they were served by the backend at /uploads/avatars/... but Next.js (the public-facing server) had no rule to proxy that path through to the backend, so every uploaded image silently 404d and fell back to the initial. Image URL mode worked around this by coincidence, since it points at an external absolute URL rather than this internal path.",
      "The Users list page (both table and card views) fetched avatarUrl correctly but never passed it to the avatar component - custom avatars only appeared once you opened a user's detail page",
      "Fixed a sizing conflict in the shared Avatar component: passing both a size preset and a custom className with different width/height created two independent, conflicting size sources (outer wrapper vs inner image container), which is what caused the distorted/stretched look on the user detail page header"
    ],
    "miscChores": []
  },
  {
    "version": "1.5.0",
    "date": "2026-07-06",
    "title": "Custom profile pictures for users and groups",
    "features": [
      "Users and groups can now have a custom picture instead of the generated color/initial avatar",
      "Set it via an image URL, or upload a file directly (JPEG/PNG/GIF/WEBP, 5MB limit)",
      "Group cloning now carries over the avatar from the original group"
    ],
    "bugFixes": [],
    "miscChores": []
  },
  {
    "version": "1.4.1",
    "date": "2026-07-06",
    "title": "README overhaul + proper credits",
    "features": [],
    "bugFixes": [],
    "miscChores": [
      "Rewrote README to reflect the actual current feature set (Vault, badges, redesign, backup export)",
      "Added a dedicated Credits section: iamneur0 (original Syncio creator), Avangelista (Nuvio integration concepts), Sonicx161/AIOManager (Vault design inspiration)"
    ]
  },
  {
    "version": "1.4.0",
    "date": "2026-07-06",
    "title": "Vault backup export (foundation for external backup + Bitwarden sync)",
    "features": [
      "Vault entries now export to a decrypted JSON snapshot nightly, written to data/backup/vault/ on the server, ready to be pulled off-server for backup or synced into an external password manager",
      "On-demand backup via POST /api/vault/backup-now instead of waiting for the nightly schedule"
    ],
    "bugFixes": [],
    "miscChores": []
  },
  {
    "version": "1.3.3",
    "date": "2026-07-06",
    "title": "Fix Vault category tabs overlapping on mobile",
    "features": [],
    "bugFixes": [
      "Vault's category filter used equal-width flex tabs with no horizontal scroll, so on narrow phone screens all 10 category labels squeezed into tiny slots and wrapped/overlapped illegibly. Switched to the existing responsive tab component, which shows a clean dropdown on mobile and the normal tab row on desktop."
    ],
    "miscChores": []
  },
  {
    "version": "1.3.2",
    "date": "2026-07-06",
    "title": "Provider badges + Vault category update",
    "features": [
      "Stremio/Nuvio badge now shown next to every user \u2014 in the users list (table and card views) and on the user detail page",
      "Vault categories: replaced Torrent Indexers and Subtitles with dedicated Stremio and Nuvio categories"
    ],
    "bugFixes": [
      "providerType was already being fetched from the database in several places but was never actually included in the user list/detail API responses \u2014 fixed"
    ],
    "miscChores": []
  },
  {
    "version": "1.3.1",
    "date": "2026-07-06",
    "title": "Critical fixes: rate limiter crash, Nuvio auth root cause",
    "features": [],
    "bugFixes": [
      "Fixed a crash on every rate-limited API request (including Nuvio connect) caused by 'trust proxy' being set to trust all hops instead of exactly one (Traefik) \u2014 express-rate-limit refuses to run with that permissive a setting",
      "Found and fixed the real cause of Nuvio login failures: the default backend URL pointed at a decommissioned raw Supabase project URL; corrected to Nuvio's actual current endpoint (api.nuvio.tv)",
      "Nuvio auth errors now surface the real reason from Nuvio's servers instead of a generic 'Authentication failed' message"
    ],
    "miscChores": []
  },
  {
    "version": "1.3.0",
    "date": "2026-07-06",
    "title": "Visual redesign: new default theme, bolder look, nav fixes",
    "features": [
      "New 'Slick' theme (bold violet + electric cyan) as the new default look, alongside all 7 original themes",
      "Actually loaded the Space Grotesk / Outfit font pairing the design system was already configured for but never loading",
      "Every card now has a gradient accent stripe; page titles render as gradient text; sidebar logo and panel redesigned with real depth/glow",
      "Sidebar highlighting is now purely hover-driven \u2014 the bold colored pill only appears on real-time cursor hover, any item, with a subtle 'current page' marker instead of a permanent fill"
    ],
    "bugFixes": [
      "Fixed a sidebar highlight that could get stuck lit after moving the mouse, caused by fragile JS-driven hover state \u2014 replaced with plain CSS :hover",
      "Fixed a missing date-fns dependency that had been silently dropped somewhere along the way"
    ],
    "miscChores": []
  },
  {
    "version": "1.2.0",
    "date": "2026-07-05",
    "title": "Credential Vault + full SlickSync rebrand",
    "features": [
      "New Vault: encrypted credential storage with expiry tracking, active-checks (Real-Debrid, TorBox, Newznab indexers, generic HTTP, TCP reachability), and ntfy/Discord notifications on expiry or failure",
      "Complete rebrand from Syncio to SlickSync across every backend and frontend string, log message, notification footer, and the Nuvio device name"
    ],
    "bugFixes": [],
    "miscChores": [
      "Renamed the project standalone from its upstream fork lineage"
    ]
  },
  {
    "version": "1.1.0",
    "date": "2026-07-05",
    "title": "Nuvio provider support",
    "features": [
      "Nuvio added as a first-class provider alongside Stremio \u2014 OAuth device-code (QR) and email/password login, in the Add User flow",
      "Sync engine, watch-history metrics, user expiration, and library read/backup all made provider-aware end to end",
      "Addon snapshots: save any user's or group's addon set as a reusable template and deploy it to anyone",
      "Provider parity view and per-group activity dashboard (member roster, addon counts, real watch-time)",
      "Anti-lockout encryption key: auto-generates and persists if unset, keeps the old key as a fallback if rotated"
    ],
    "bugFixes": [
      "The general API rate limiter was defined but never actually mounted upstream \u2014 enabled it, plus stricter limits on credential/OAuth endpoints"
    ],
    "miscChores": []
  }
]
